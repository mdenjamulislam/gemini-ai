// I want to create generate text from text-and-image inout using google gemini. I have an API key.

const TextFromTextImage = () => {
    return (
        <div>
            
        </div>
    );
};

export default TextFromTextImage;